require = require('@std/esm')(module);

const RequestResponse = require('../RequestResponse').default;

describe('RequestResponse', () => {
  beforeEach(() => {
    //
  });

  test('It writes the data to a file.', () => {
    const RequestResponseObj = new RequestResponse();
  });
});
